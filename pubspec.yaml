name: costreveal_sach_engine
description: "CostReveal: The Dirty Tricks Detector - NGO Field Tool for Predatory Loan Detection"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # UI Dependencies
  cupertino_icons: ^1.0.8
  fl_chart: ^1.2.0
  google_fonts: ^8.2.1

  # Backend Dependencies (M1 - Engine & Compliance)
  provider: ^6.1.5+1
  pdf: ^3.11.0              # ← ADDED: PDF Evidence Generator

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true